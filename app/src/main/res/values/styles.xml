<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="LL">
        <item name="android:orientation">vertical</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:padding">@dimen/padding</item>
        <item name="android:background">@drawable/background</item>
    </style>
    <style name="LL.secondary">
        <item name="android:background">@drawable/background_blur</item>
    </style>

    <style name="margins">
        <item name="android:layout_marginBottom">@dimen/activity_vertical_margin</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>
    <style name="margins.title">
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
    </style>
    <style name="margins.title.text">
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
    <style name="margins.text">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:textColor">@android:color/darker_gray</item>
    </style>
    <style name="margins.title.buttons">
        <item name="android:layout_width">@dimen/buttons_width</item>
    </style>

</resources>
